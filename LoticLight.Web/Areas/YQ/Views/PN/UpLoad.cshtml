@{
    ViewBag.Title = "Form";
    Layout = "~/Views/Shared/_LayoutFormFile.cshtml";
}
<script src="~/Content/lib/datepicker/WdatePicker.js"></script>


<script>
    //获取url参数
    var keyValue = request('keyValue');
    $(function () {
        initControl();
        initUserList();
    })
    //初始化控件
    function initControl() {
      

    }
    //初始化加载用户列表
    function initUserList() {
        
    }
    //保存表单
    function AcceptClick() {
        if (!$('#form1').Validform()) {
            return false;
        }
        
        $.SaveFormFile({
            url: "/YQ/PN/Upload",
            param: null,
            loading: "正在保存数据...",
            success: function () {
                top.frames[tabiframeId()].$("#gridTable").trigger("reloadGrid");
            }
        })
    }

    
</script>
<div style="margin-left:10px; margin-top:20px; margin-right:30px;">
    <table class="form">
        <tr>
            <td class="formTitle" >附件<font face="宋体" ></font></td>
            <td class="formValue" colspan="3">
                <div>
                    <input type="file" name="file" id="file" isvalid="yes" checkexpession="NotNull" />
                </div>
            </td>
        </tr>
        <tr>
            <td class="formTitle" >数据所在<br/>工作表?<font face="宋体">*</font></td>
            <td class="formValue" colspan="3" >
                <input id="sheetName" name="sheetName" value="Sheet1" type="text" onblur="" class="form-control"  isvalid="yes" checkexpession="NotNull" maxlength="90" />
            </td>
            
        </tr>
        
        
    </table>
</div>


